// Diese Datei wurde im Rahmen des Refactorings geleert.
// Die Logik wurde in dedizierte Controller verschoben (z.B. roomController.js, categoryController.js etc.).

class SettingsController {
  // Leer - keine Methoden mehr hier
}

module.exports = new SettingsController();
