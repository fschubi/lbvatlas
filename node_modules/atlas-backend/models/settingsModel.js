// Diese Datei wurde im Rahmen des Refactorings geleert.
// Die Logik wurde in dedizierte Models verschoben (z.B. roomModel.js, categoryModel.js etc.).

class SettingsModel {
  // Leer - keine Methoden mehr hier
}

module.exports = new SettingsModel();
